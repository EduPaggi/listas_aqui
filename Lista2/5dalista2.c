#include <stdio.h>
#include <stdlib.h>

int main(){

    int soma;
    int lim = 100;

    for(int i = 0; i <= 101; i++){
        if(i % 2 == 0){
            soma = soma + i;
        } else{}
        printf("%d ", soma);
    }
}
